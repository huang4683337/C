
#include <stdio.h>
int main(){
	printf("我是c语言唯一的主函数\n");
	return 0;
}

/**
 * 主函数 main 是c语言中唯一的入口
 *
 * int 就是主函数的类型
 *
 * printf() 是格式输出函数 就是你要在屏幕上输出的信息
 *
 * return 是函数的返回值  函数类型不同 返回的值也就不同
 *
 * /n 是转意字符  可在WIKI中查看
 *
 * 注意:c程序的运行一定是从主函数开始的
 */